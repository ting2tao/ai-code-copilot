# 测试 Spec：{变更名}

> **关联 Spec**：`spec.md`
> **Red/Green 铁律**：新增或修正测试必须先验证 RED（测试确实失败），再实施代码使其 GREEN。跳过 RED 的测试视为无效。
> **覆盖率门禁**：默认 statement ≥ 80%，branch ≥ 70%；如项目规则另有约定，以 `.ai_code_copilot/rules/project-context.md` 为准。

---

## 0. 项目测试上下文

> 从 `.ai_code_copilot/rules/project-context.md` 读取并填写。多技术栈项目按模块分别列出。

| 模块路径 | 技术栈/规则包 | 测试框架 | 全量测试命令 | 单测/定向测试命令 | 覆盖率命令/报告 |
|----------|---------------|----------|--------------|-------------------|----------------|
| `{module}` | `{pack}` | `{framework}` | `{test command}` | `{single test command}` | `{coverage command or report}` |

---

## 1. Agent Harness

> 测试计划必须说明 Agent 如何看见失败、验证修复和定位下一步。

| 项 | 内容 |
|----|------|
| Agent 可见证据 | {测试输出、构建输出、日志、截图、CI/PR 链接等} |
| 必跑验证命令 | `{command}` |
| 可观测信号 | {日志字段、指标、trace/span、浏览器截图或 DOM 状态；无则填"无"} |
| 失败自诊断入口 | {失败时先看哪些命令、日志、文件或 dashboard} |
| 人工确认项 | {Agent 无法直接验证的外部条件；无则填"无"} |

---

## 2. Loop Evidence

> 测试阶段记录 Done Signal 是否可信，以及 Guardrails 是否防止 Agent 针对验证器投机。

| 项 | 内容 |
|----|------|
| Goal observed | {本轮测试要证明的目标} |
| Done Signal | `{证明完成的命令、断言、指标或截图}` |
| Guardrail checks | {证明没有删除测试、降低断言、绕过校验或跳过检查} |
| Fallback exercised | {失败时是否触发降级/停机/人工确认；未触发则填"未触发"} |
| Memory update | {需要沉淀的测试/规则/知识；无则填"无"} |

---

## P0 核心逻辑测试（必测）

> 覆盖主流程、关键边界和失败分支。优先选择最快、最稳定的测试层级。

### TC-P0-01：{测试场景名}

**模块**：`{module path}`
**测试文件**：`{test file path}`
**测试目标**：`{function / class / component / endpoint}`
**前置条件**：{数据准备、依赖替身、环境配置}
**操作**：{调用方式或用户行为}
**预期结果**：{断言内容}
**RED 证据**：`{先失败的命令和失败摘要}`
**GREEN 证据**：`{修复后通过的命令和输出摘要}`

### TC-P0-02：{异常或边界场景}

**模块**：`{module path}`
**测试文件**：`{test file path}`
**触发条件**：{异常/边界输入}
**预期结果**：{错误、返回值、状态、日志或副作用断言}
**RED 证据**：`{先失败的命令和失败摘要}`
**GREEN 证据**：`{修复后通过的命令和输出摘要}`

---

## P1 集成/数据/状态测试（应测）

> 覆盖跨边界协作：数据访问、外部接口适配、状态变化、权限校验、组件交互等。

### TC-P1-01：{集成场景名}

**模块**：`{module path}`
**测试文件**：`{test file path}`
**依赖边界**：{database / api / filesystem / browser / queue / cache / other}
**操作**：{执行步骤}
**预期结果**：{持久化结果、接口响应、状态变化或交互断言}

---

## P2 入口/端到端测试（选测）

> 覆盖用户入口、HTTP/API、CLI、页面交互或端到端主路径。若不需要，必须说明原因。

### TC-P2-01：{入口场景名}

**入口**：`{route / command / page / endpoint}`
**测试文件**：`{test file path}`
**操作**：{请求、命令或用户行为}
**预期结果**：{响应、页面状态、输出或副作用}

---

## 不测试项

| 项目 | 原因 | 风险接受人/确认方式 |
|------|------|-------------------|
| `{item}` | `{reason}` | `{confirmation}` |

---

## 覆盖率目标

| 模块/文件 | Statement | Branch | 说明 |
|-----------|-----------|--------|------|
| `{module or file}` | ≥ 80% | ≥ 70% | `{notes}` |

---

## 实际测试结果

> /test 或 /apply 完成后填写，必须记录实际命令输出摘要。

| 命令 | 结果 | 输出摘要 |
|------|------|----------|
| `{command}` | PASS / FAIL | `{actual output summary}` |
